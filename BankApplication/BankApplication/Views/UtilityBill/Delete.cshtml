@model BankApplication.Models.UtilityBillModel

@{
    ViewBag.Title = "Delete";
}

<h2>Pay Bill</h2>

<h3>Are you sure you want to pay this Bill?</h3>
<div>
    <h4>UtilityBillModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AccountToPayID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AccountToPayID)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.AmountToPay)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AmountToPay)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="PayBill" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
